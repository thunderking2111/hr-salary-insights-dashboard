# Backend development-only dependencies.
# Includes runtime deps via -r, plus testing and linting tools.
#
# Install with:
#   pip install -r backend/requirements-dev.txt

-r requirements.txt

pytest==9.0.3
pytest-django==4.12.0
pytest-env==1.6.0
ruff==0.15.14
pre-commit==4.6.0
