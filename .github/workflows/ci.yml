name: CI

on:
  push:
    branches: [master, main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Detect changed paths
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      frontend: ${{ steps.filter.outputs.frontend }}
      shared: ${{ steps.filter.outputs.shared }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            backend:
              - 'backend/**'
              - 'pyproject.toml'
              - '.pre-commit-config.yaml'
            frontend:
              - 'frontend/**'
            shared:
              - '.github/**'
              - 'README.md'
              - 'docs/**'
              - 'AGENTS.md'

  backend:
    name: Backend (lint + tests)
    runs-on: ubuntu-latest
    needs: changes
    if: |
      github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.backend == 'true' ||
      needs.changes.outputs.shared == 'true'
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements-dev.txt

      - name: Install backend dependencies
        run: pip install -r backend/requirements-dev.txt

      - name: Cache pre-commit environments
        uses: actions/cache@v4
        with:
          path: ~/.cache/pre-commit
          key: pre-commit-${{ runner.os }}-py3.12-${{ hashFiles('.pre-commit-config.yaml') }}

      - name: Lint (pre-commit)
        run: pre-commit run --all-files --show-diff-on-failure

      - name: Backend tests
        working-directory: backend
        run: pytest -v

  frontend:
    name: Frontend (lint + tests + build)
    runs-on: ubuntu-latest
    needs: changes
    if: |
      github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.frontend == 'true' ||
      needs.changes.outputs.backend == 'true' ||
      needs.changes.outputs.shared == 'true'
    steps:
      - uses: actions/checkout@v4

      - name: Check frontend project exists
        id: frontend_project
        run: |
          if [ -f frontend/package-lock.json ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
            echo "Skipping frontend job: frontend/package-lock.json not found (scaffold not landed yet)."
          fi

      - name: Set up Node
        if: steps.frontend_project.outputs.present == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        if: steps.frontend_project.outputs.present == 'true'
        working-directory: frontend
        run: npm ci

      - name: ESLint
        if: steps.frontend_project.outputs.present == 'true'
        working-directory: frontend
        run: npm run lint

      - name: Frontend tests
        if: steps.frontend_project.outputs.present == 'true'
        working-directory: frontend
        run: npm test

      - name: Production build
        if: steps.frontend_project.outputs.present == 'true'
        working-directory: frontend
        run: npm run build
